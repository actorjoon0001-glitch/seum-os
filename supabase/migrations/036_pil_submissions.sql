-- 036_pil_submissions.sql
-- 외부 협력 건축사(필건축사) 도면 업로드 메타데이터.
-- "부서별 업무 > 설계팀 > 필건축사" 메뉴에서 PDF/DWG 도면을
-- 직접 업로드하면 설계팀 + 시공팀 전원에게 인앱/브라우저 알림이 발사된다.
-- 파일 자체는 Supabase Storage 'contract_files' 버킷의
-- 'pil_submissions/...' 경로에 저장.
-- (035_haeyoung_submissions.sql 과 동일 구조 — 외부 협력 건축사별 분리 테이블)

CREATE TABLE IF NOT EXISTS public.pil_submissions (
  id bigint PRIMARY KEY GENERATED BY DEFAULT AS IDENTITY,
  title text NOT NULL,
  description text,
  file_name text,
  file_url text,
  file_path text,
  file_size bigint,
  file_type text,
  contract_local_id text,
  uploaded_by_name text,
  uploaded_at timestamptz DEFAULT now(),
  is_deleted boolean DEFAULT false
);

COMMENT ON TABLE public.pil_submissions IS '필건축사(외부 협력) 도면 업로드 메타데이터.';
COMMENT ON COLUMN public.pil_submissions.contract_local_id IS '연결된 계약 local_id (선택)';
COMMENT ON COLUMN public.pil_submissions.file_url IS 'Supabase Storage public URL';
COMMENT ON COLUMN public.pil_submissions.uploaded_by_name IS '업로드 주체 표시명 (필건축사 본인 또는 대리 업로드자)';

CREATE INDEX IF NOT EXISTS idx_pil_submissions_uploaded_at
  ON public.pil_submissions(uploaded_at DESC);
CREATE INDEX IF NOT EXISTS idx_pil_submissions_contract
  ON public.pil_submissions(contract_local_id);

ALTER TABLE public.pil_submissions ENABLE ROW LEVEL SECURITY;

DROP POLICY IF EXISTS "pil_submissions_all_read" ON public.pil_submissions;
CREATE POLICY "pil_submissions_all_read"
  ON public.pil_submissions FOR SELECT
  USING (true);

DROP POLICY IF EXISTS "pil_submissions_all_write" ON public.pil_submissions;
CREATE POLICY "pil_submissions_all_write"
  ON public.pil_submissions FOR ALL
  USING (true)
  WITH CHECK (true);

-- Realtime 활성화 (업로드 즉시 다른 세션 목록도 갱신)
DO $$
BEGIN
  IF NOT EXISTS (
    SELECT 1 FROM pg_publication_tables
    WHERE pubname = 'supabase_realtime'
      AND schemaname = 'public'
      AND tablename = 'pil_submissions'
  ) THEN
    ALTER PUBLICATION supabase_realtime ADD TABLE public.pil_submissions;
  END IF;
END$$;
